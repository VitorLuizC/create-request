{
  "extends": "../../tsconfig.build.json",
  "compilerOptions": {
    // Type declaration settings
    "declarationDir": "./types",

    // ECMAScript library settings
    "lib": ["ES5", "DOM", "ES2015.Promise"]
  },
  "include": ["./src/**/*.ts"]
}
